  
加雷达训练
 1) 启动 screen

  screen -S detr_revp_5ep

  2) 在 screen 里执行训练

cd /tmp && python -u /root/autodl-tmp/DrivingSDK/model_examples/Deformable-DETR/detr/main.py --device npu --dataset_file coco --coco_path /root/autodl-tmp/DrivingSDK/model_examples/Deformable-DETR/data/WaterScenes_real_tir --output_dir /tmp/ws_real_tir_train --epochs 10 --batch_size 2 --num_workers 0 --tir_dropout 0 --tir_strict 1 2>&1 | tee /tmp/ws_real_tir_train.log

  3) 退出 screen（不中断训练）
  按：Ctrl+A 然后 D

  4) 以后查看/恢复

  screen -r detr_revp_5ep

  5) 查看后台列表

  screen -ls


  训练
 creen -r ddetr_train 
  screen -S ddetr_train -X quit >/dev/null 2>&1 || true
screen -dmS ddetr_train bash -lc 'cd /root/autodl-tmp/DrivingSDK/model_examples/Deformable-DETR/detr && bash test/train_8p_full.sh --data_path=/root/autodl-tmp/DrivingSDK/model_examples/Deformable-DETR/data --epochs=1 --batch_size=2'
screen -ls | cat
  1) 可视化整个数据集（按 image/*.jpg 全部）                                                                   
                                                                                                               
  cd ~/autodl-tmp/DrivingSDK/model_examples/Deformable-DETR/detr                                               
  DATA_ROOT=../data/waterscenes-coco                                                                           
  OUT_DIR=visualization_all                                                                                    
                                                                                                               
  mkdir -p "$OUT_DIR"                                                                                          
  ls "$DATA_ROOT/image"/*.jpg | xargs -n1 basename > /tmp/waterscenes_all.txt                                  
                                                                                                               
  python tools/radar_overlay_viz.py --data_root "$DATA_ROOT" --calib_dir "$DATA_ROOT/calib" --batch_list /tmp/ 
  waterscenes_all.txt --out_dir "$OUT_DIR" --stats_json --continue_on_error                                    
                                                                                                               
  2) 可视化 10 张                                                                                              
                                                                                                               
  cd ~/autodl-tmp/DrivingSDK/model_examples/Deformable-DETR/detr                                               
  DATA_ROOT=../data/waterscenes-coco                                                                           
  OUT_DIR=visualization_10                                                                                     
                                                                                                               
  mkdir -p "$OUT_DIR"                                                                                          
  ls "$DATA_ROOT/image"/*.jpg | head -n 10 | xargs -n1 basename > /tmp/waterscenes_10.txt                      
                                                                                                               
  python tools/radar_overlay_viz.py --data_root "$DATA_ROOT" --calib_dir "$DATA_ROOT/calib" --batch_list /tmp/ 
  waterscenes_10.txt --out_dir "$OUT_DIR" --stats_json --continue_on_error                                     
                                                                                                               
  3) 可视化 20 张                                                                                              
                                                                                                               
  cd ~/autodl-tmp/DrivingSDK/model_examples/Deformable-DETR/detr                                               
  DATA_ROOT=../data/waterscenes-coco                                                                           
  OUT_DIR=visualization_20                                                                                     
                                                                                                               
  mkdir -p "$OUT_DIR"                                                                                          
  ls "$DATA_ROOT/image"/*.jpg | head -n 20 | xargs -n1 basename > /tmp/waterscenes_20.txt                      
                                                                                                               
  python tools/radar_overlay_viz.py --data_root "$DATA_ROOT" --calib_dir "$DATA_ROOT/calib" --batch_list /tmp/ 
  waterscenes_20.txt --out_dir "$OUT_DIR" --stats_json --continue_on_error                                     
                                                                                                               
  提示：                                                                                                       
                                                                                                               
  - 如果你想要“raw+synced”两种图，加上：--overlay_mode both --transform_set val                                
  - 如果你想固定输出到同一个目录，直接把 OUT_DIR 改成你想要的路径即可。                                        
 